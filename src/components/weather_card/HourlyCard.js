import React, {Component, Fragment, useEffect} from 'react';



const HourlyCard = ({currentHour, address}) => {
  // TODO: return card with feels like temp, weather name, time of day
  // wind too maybe

};


export default HourlyCard;
